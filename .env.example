# Debian Docker Desktop Configuration
# Copy this file to .env and customize as needed

# =============================================================================
# SECURITY NOTES (read before exposing publicly)
# =============================================================================
# KasmVNC (port 6901): Password-protected via VNC_PW. Change the default!
# NoMachine (port 4000): Requires USER_PASSWORD for PAM login. Safe to expose.
# XRDP (port 3389): Requires USER_PASSWORD for PAM login. Safe to expose.
# VNC raw (port 5901): Only password-protected, no encryption. Avoid exposing.
#
# For public exposure, recommended setup:
#   1. Set strong VNC_PW and USER_PASSWORD
#   2. Put port 6901 behind a reverse proxy (Caddy/nginx) with HTTPS
#   3. Or use Tailscale/WireGuard VPN and don't expose ports directly
#   4. Consider enabling KasmVNC SSL: KASMVNC_ENABLE_SSL=true

# =============================================================================
# USER CONFIGURATION
# =============================================================================
# Username for the desktop user (default: user)
USERNAME=user

# User and Group IDs (should match your host user for volume permissions)
USER_UID=1000
USER_GID=1000

# Linux user password (required for NoMachine/XRDP login, optional for KasmVNC-only use)
# IMPORTANT: Change this before exposing publicly
USER_PASSWORD=

# =============================================================================
# DISPLAY CONFIGURATION
# =============================================================================
# Screen resolution (WIDTHxHEIGHTxDEPTH)
RESOLUTION=1920x1080x24

# VNC display number
DISPLAY=:1

# Timezone
TZ=UTC

# =============================================================================
# VNC CONFIGURATION
# =============================================================================
# VNC password (for KasmVNC)
VNC_PW=vncpassword

# View-only VNC password (optional, leave empty to disable)
VNC_VIEW_ONLY_PW=

# VNC ports
VNC_PORT=5901
VNC_WEB_PORT=6901

# =============================================================================
# KASMVNC CONFIGURATION
# =============================================================================
# Enable SSL with self-signed certificate (set to true for HTTPS on port 6901)
KASMVNC_ENABLE_SSL=false

# Enable audio streaming
KASM_SVC_AUDIO=1
KASM_SVC_AUDIO_INPUT=1

# Enable file transfer
KASM_SVC_UPLOADS=1
KASM_SVC_DOWNLOADS=1

# =============================================================================
# PLUGIN CONFIGURATION
# Comma-separated list of plugins to install on container start
# Available: brew, chrome, xrdp, nomachine, cursor, vscode, claude-code, docker
# =============================================================================
PLUGINS=

# Plugin-specific ports
XRDP_PORT=3389
NOMACHINE_PORT=4000

# =============================================================================
# HOMEBREW CONFIGURATION
# =============================================================================
HOMEBREW_NO_AUTO_UPDATE=1
HOMEBREW_NO_ANALYTICS=1

# =============================================================================
# CONTAINER CONFIGURATION
# =============================================================================
# Container name prefix
COMPOSE_PROJECT_NAME=desktop

# Image tag to use
IMAGE_TAG=latest
